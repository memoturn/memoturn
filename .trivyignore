# CVE exceptions for the image scan (.github/workflows/docker-build.yml + image-scan.yml).
# One id per line, with the reason and a re-check date, e.g.:
# CVE-2026-12345  # false positive in <package>; not reachable from Bun runtime — recheck 2026-12
